# Plant Care

> **Documentation has moved.** The canonical project entry is **[ReadMe.md](ReadMe.md)**.  
> **Main tutorial & instructions:** **[docs/INDEX.md](docs/INDEX.md)**

Quick start:

```bash
cp .env.example .env && npm install && npm run db:generate && npm run db:push && npm run db:seed
npm run dev:api   # :3001
npm run dev:web   # :5173
```

Open http://localhost:5173 · API docs http://localhost:3001/api/docs
