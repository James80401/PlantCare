# Species classification

> **Navigation:** [Care guides INDEX](INDEX.md) · Source: `prisma/data/care-guide-classify.ts`

Categories include: `succulent`, `cactus`, `fern`, `palm`, `orchid`, `aroid`, `herb`, `vegetable`, `fruit`, `vine`, `citrus`, `flowering`, `moisture`, `houseplant`.

Used in **seed** to pick template variants (e.g. mist section text).

Runtime duplicate: `apps/api/src/care-guides/growing-environment.ts` → `classifySpeciesForCare()`.
