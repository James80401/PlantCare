# Stripe integration

> **Navigation:** [Integrations INDEX](INDEX.md)

```env
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID_PREMIUM=
```

`BillingService` — checkout session + webhook updates `planTier`.

Dev: `ALL_USERS_PREMIUM=true` bypasses payment.
