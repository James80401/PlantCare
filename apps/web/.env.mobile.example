# Copy to apps/web/.env.local before npm run mobile:sync or mobile:release:android
# Values are baked into the Capacitor web bundle at build time.

VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
VITE_ENABLE_PREMIUM_BILLING=false
