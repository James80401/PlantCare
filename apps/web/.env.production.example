# Copy to apps/web/.env.local before mobile:sync or production web Docker build.
# Must be a URL your users' phones and browsers can reach (HTTPS in production).

VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
